This exploration focuses on a naive controller-oriented design with anemic models and shared code implemented as helper modules. By all regards, this would be considered what *not* to do. Consider this as a starting point if you desire to treat this as a refactoring kata.

This is implemented in Rails 4.1.5 defaults.
